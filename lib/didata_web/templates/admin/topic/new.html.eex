<h1 class="title">New Topic</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.admin_topic_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.admin_topic_path(@conn, :index), class: "button" %></span>
