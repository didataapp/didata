<h1 class="title">New Objective</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.admin_objective_path(@conn, :create)) %>

<div><%= link "Back", to: Routes.admin_objective_path(@conn, :index), class: "button" %></div>
