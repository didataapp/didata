<h1 class="title">Edit Objective</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.admin_objective_path(@conn, :update, @objective)) %>

<span><%= link "Back", to: Routes.admin_objective_path(@conn, :index), class: "button" %></span>
